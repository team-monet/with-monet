# Monet — memory migration → moved

This playbook has been **superseded by [`consolidate-memory.md`](consolidate-memory.md)**.

Assigning circles to legacy `"default"` memory is now just one part of a broader interactive flow — **capture memory from any source (legacy Monet, other agents' rule files, tool-managed stores, notes/docs) into Monet, then retire the source** so Monet is the single place your sessions read from.

Use **[`consolidate-memory.md`](consolidate-memory.md)**.
