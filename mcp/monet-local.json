{
  "_comment": "MCP config template for the Monet Local server. Install it first: `npm i -g @team-monet/local` (or use the npx command below). Copy into your host's MCP config (.mcp.json for Claude Code). Storage defaults to <repo>/.monet; set env.MONET_STORAGE_DIR to override.",
  "mcpServers": {
    "monet-local": {
      "command": "monet-local",
      "args": ["start"]
    }
  },
  "_npx_alternative": { "command": "npx", "args": ["-y", "@team-monet/local", "start"] }
}
